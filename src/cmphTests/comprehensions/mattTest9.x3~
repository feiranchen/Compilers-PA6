baseChar := 48;
fun stringOfInt(n : Integer) : String {
    if (n == 0) {
        return string([character(baseChar)]);
    }
    digit := n % 10;
    for (i in digit) {
        divide := n / 10;
        for (j in divide) {
            if (j == 0) {
                return string([character(baseChar + i)]);
            }
            return string(stringOfInt(j) ++ [character(baseChar + i)]);
        }
    }
    return "error";
}

lst := [for (x in [for (x in 1..10) x]) for (y in [for (y in [for (y in 1..5) y,1]) 2*y]) if (x != y) y];

output := [];
for (number in lst){
    output := output ++ [stringOfInt(number)];
}


return out;
